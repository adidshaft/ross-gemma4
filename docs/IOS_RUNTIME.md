# iOS Gemma 4 Runtime

## Current Status

Ross no longer uses the old `Gemma4DemoRuntime`-only path described in earlier notes. The iOS app now has four runtime lanes:

| Lane | Code path | Status |
| --- | --- | --- |
| Deterministic development runtime | `AlphaLocalModelRuntime` deterministic provider | Used by tests and local CI-style validation. It must not be claimed as real model execution. |
| GGUF llama.cpp runtime | `AlphaLlamaCppEngine` and `AlphaLlamaCppProvider` | Simulator smoke passed on June 2, 2026 with a local GGUF developer artifact. The project is now pinned to `llama.swift` `2.9672.0` as of June 17, 2026, which resolves the newer upstream `llama.cpp` Apple XCFramework build `b9672`, and the GGUF lane continues to expose speculative draft acceleration for Gemma 4 MTP companions while the visible 3-pack ladder points at the newer Unsloth QAT GGUF repos. |
| MLX local-directory runtime | `AlphaMLXLocalProvider` with `mlx-swift-lm` | Supported as an iPhone/macOS path for MLX model directories. The installer can unpack ZIP-packaged MLX directories, and the E4B / 12B MLX tiers now accept official Gemma 4 assistant draft companions for speculative decoding. Physical iPhone validation is still pending. |
| Apple on-device assistant path | `apple_foundation_models` pack mode | Available only when the OS/device supports it and explicit runtime checks pass. |

## Verified Evidence

- Swift package tests cover registry parsing, artifact validation, download-state recovery, Ask routing, imported text/PDF/image behavior, and multilingual source-preserving fallback.
- Local model smoke runs now emit `ROSS_RUNTIME_IDENTITY` before generation. Treat this as the authority for provider name, requested runtime, actual runtime, active pack runtime, artifact kind/path type, acceleration mode, draft metadata including draft artifact path type, safe draft validator detail, runtime error detail, context size, GPU/offload summary, fallback state, and availability.
- Explicit debug/smoke runtime overrides do not borrow artifact paths from an installed pack with a different runtime. MLX, GGUF, and CoreAI/CoreML lanes must use an explicit debug artifact path with a matching artifact kind, a matching installed pack, or a system-model sentinel where that runtime supports one.
- If an active runtime fails health before generation, smoke now emits the same identity marker with `available=false`, the actual provider object selected for that failed path, the concrete error category, and a safe `runtime_error_detail` category when available. This is expected for missing or malformed MLX/CoreAI artifacts and is still routing evidence, not a generation benchmark.
- Smoke pass/fail lines include stage-prefixed benchmark fields such as `source_input_tokens`, `source_output_tokens`, `source_token_speed`, `source_first_token_ms`, `source_measured_tokens`, `source_acceleration`, `source_draft_tokens`, and `source_draft_model`. Prefer measured token fields when `*_measured_tokens=true`; otherwise treat counts/speeds as runtime estimates.
- Each app smoke run emits `ROSS_LOCAL_MODEL_SMOKE_BENCHMARK_MATRIX` before generation. `full` covers source-bound English legal QA, source-bound Bengali/Hindi/Tamil/Telugu legal QA, and a no-document open query; `quick` covers English source-bound QA plus the open query; `quick_low_context`/`quick-low-context` uses the same two-case quick matrix and JSON answer schema while applying the smoke-only 1024-token context cap, 256-token prompt batch, and 64-token physical batch for safer GGUF baseline validation on memory-constrained devices; `mtp_quick` uses the same short matrix with 12-token plain-text answer caps plus those same low-context/batch caps for safer draft-acceleration activation validation. Constrained 7GB-class E4B keeps the older 128-token prompt batch so its draft memory-policy block remains conservative. The helpers also accept the `mtp-quick` alias, and benchmark summaries guard both spellings as MTP proof profiles.
- Smoke helpers reject unsupported `--smoke-profile` values before launch, so a typo cannot silently fall back to the app's default `full` matrix and contaminate quick/MTP benchmark comparisons.
- After identity guardrails pass, smoke helpers print `ROSS_SMOKE_BENCHMARK_SUMMARY` with the verified runtime identity, acceleration metadata, smoke profile, benchmark matrix profile/cases/stages, elapsed time, and any stage token/acceleration metrics from the pass line. Use that summary for benchmark tables instead of scraping unguarded app logs. A pass marker without `ROSS_LOCAL_MODEL_SMOKE_BENCHMARK_MATRIX` is rejected as `missing_benchmark_matrix`; a pass marker whose runtime disagrees with `ROSS_RUNTIME_IDENTITY actual_runtime` is rejected as `benchmark_runtime_mismatch`; an identity marker without `requested_runtime` is rejected as `benchmark_requested_runtime_missing`; an identity marker whose `requested_runtime` disagrees with `actual_runtime` is rejected as `benchmark_requested_runtime_mismatch`; a pass marker whose `requested_runtime` disagrees with the verified runtime identity is rejected as `benchmark_pass_requested_runtime_mismatch`; an identity marker without `pack_runtime` is rejected as `benchmark_pack_runtime_missing`; an identity marker whose `pack_runtime` disagrees with `actual_runtime` is rejected as `benchmark_pack_runtime_mismatch`; an identity runtime outside the known GGUF, MLX, or Apple Foundation/CoreAI lanes is rejected as `benchmark_runtime_unsupported`; an otherwise passing identity without provider, `checksum_verified=true`, positive `context_tokens`, or `gpu_offload` evidence is rejected as `benchmark_runtime_identity_missing`. GGUF summaries require a `.gguf` artifact label/path; CoreAI/Foundation `system_model` summaries require a true `system-model` or `system://...` sentinel path; file-backed adapters must use adapter artifact kinds instead.
- Benchmark summaries now require every matrix stage to carry numeric `*_input_tokens`, positive `*_output_tokens`, positive `*_token_speed`, non-negative `*_first_token_ms`, and an explicit `*_measured_tokens=true|false` flag. Stage output tokens must also stay within that stage's matrix `max_tokens` cap. Missing, placeholder, over-cap, or internally inconsistent stage metrics are rejected as `benchmark_stage_metrics_missing`, so `Tokens processed` and `Token speed` are benchmark evidence only when tied to a matrix stage and verified runtime identity.
- In the product UI, answer-level `Tokens processed` and `Token speed` stay out of the main response chrome and are available from the hidden Answer Details surface through the info/context-menu action. This keeps performance metadata inspectable without adding cognitive load to every answer.
- Failed simulator, seeded-GGUF, and installed-pack smokes print `ROSS_SMOKE_FAILURE_SUMMARY` with the same runtime identity shape plus failure fields such as requested runtime, stage errors, grounding flags, source-ref retention, native-model flags, output/source char counts, warning counts, draft status, safe draft/runtime diagnostic details, and any emitted stage token/acceleration/draft metrics. Use these lines for triage only; they do not turn a failed MLX/CoreAI/MTP run into a benchmark.
- `scripts/ios-simulator-local-model-smoke.sh` provides the same pass-only identity guardrail for simulator checks. It is useful for overnight-safe GGUF/MLX/CoreAI routing checks because it uses `simctl` and never seeds or launches a physical iPhone. Use `--preflight-only` first when you only need a no-launch artifact/sentinel sanity check: it validates GGUF file shape, MLX directory shape, CoreAI/CoreML adapter shape, and `system-model`/`system://...` sentinels, then emits `ROSS_SIMULATOR_SMOKE_PREFLIGHT_OK` without launching Simulator.
- The cabled-device smoke helpers fail a run if a pass marker arrives without `ROSS_RUNTIME_IDENTITY`, or if the requested runtime does not match the actual runtime reported by the app. MLX/CoreAI/MTP numbers must not be recorded from a GGUF identity marker.
- Passing simulator and cabled-device smoke helpers also reject impossible runtime artifact identity, such as an MLX pass with a file-backed GGUF artifact or a CoreAI adapter pass that reports the system sentinel path type.
- Assistant-download smokes also fail closed for explicit runtime requests: the selected terminal job and installed pack must match the requested runtime, and the shell helper rejects unknown runtime labels, pass markers whose runtime field differs from the requested lane, or identity markers missing provider, positive context, or GPU/offload evidence. This prevents an MLX/CoreAI download smoke from passing from a previously installed GGUF pack or from a weak identity marker.
- Failed smoke runs remain failed, but the helpers now also validate any identity marker emitted before failure. This catches mislabeled failed MLX/CoreAI/MTP attempts during triage instead of leaving the mismatch hidden inside noisy logs.
- `scripts/ios-device-installed-pack-smoke.sh` also preflights the selected manifest before launching the app: MLX requires `artifactKind=mlx_directory`, GGUF requires a GGUF/local-model artifact kind, and Apple Foundation/CoreAI requires a system/foundation/CoreAI/CoreML adapter kind. This prevents an impossible manifest/runtime pair from consuming a device benchmark slot.
- The app-side installed-pack validator mirrors those manifest rules before a pack is reused or selected: MLX packs must remain `mlx_directory`, GGUF packs must remain `local_model_artifact`, and Apple Foundation/CoreAI file-backed packs must remain adapter kinds. Built-in `system_model` is accepted only for true sentinel paths such as `system-model` or `system://...`.
- Installed-pack GGUF/MTP smokes also reject manifest metadata that points at non-`.gguf` paths or implausibly small primary/draft artifacts before launching on the cabled device. Pass `--physical-memory-bytes <bytes>` to the installed-pack helper during known-device validation so constrained E4B GGUF+MTP pairs that exceed the app's draft memory budget fail before launch instead of reaching a predictable `mmap`/validator failure.
- Installed-pack MLX smokes similarly reject file-like `.gguf`/`.safetensors`/`.bin` paths and implausibly small primary/draft artifact metadata before launch; the runtime still has to prove generation under `actual_runtime=mlx_swift_lm`.
- Benchmark summaries apply the same MLX shape rule to runtime identity: `mlx_directory` identities must be directory-backed and must not use file-like `.gguf`, `.safetensors`, or `.bin` primary/draft labels.
- Installed-pack CoreAI/CoreML adapter smokes reject empty adapter metadata before launch; built-in `system_model` remains sentinel-based and must still report `artifact_path_type=system`.
- `scripts/test-ios-device-installed-pack-preflights.sh` exercises these installed-pack manifest rejections with a fake `xcrun devicectl`, so the morning helper's pre-launch gates are testable without using a physical iPhone.
- MTP proof requires `--require-draft-acceleration` with `--smoke-profile mtp_quick`/`mtp-quick`/`mtp`, plus an identity marker with `acceleration=draftModelSpeculative`, `draft_status=active`, non-`nil` `draft_tokens`, and a `.gguf` `draft_model` label. Benchmark summaries also reject any `mtp`/`mtp_quick`/`mtp-quick` profile unless the identity reports active draft acceleration, then require every stage listed in `ROSS_LOCAL_MODEL_SMOKE_BENCHMARK_MATRIX` to report matching `*_acceleration=draftModelSpeculative`, `*_draft_tokens`, and `*_draft_model`; a run where identity is active but a generation stage falls back to standard is rejected as `benchmark_draft_stage_mismatch`. If required draft acceleration is inactive before generation, the fail marker preserves `draft_model_path_type` so triage can distinguish file, directory, and missing draft companions. The simulator, installed-pack, and seeded GGUF device helpers fail before launch if `--require-draft-acceleration` is paired with `full`, `quick`, `source-only`, or `--disable-draft`, keeping MTP validation low-token and low-context by construction. The seeded GGUF device helper can copy a local `--draft-model` into the same run-scoped manifest as `draftArtifact`, while the installed-pack helper fails before launch if the selected installed manifest lacks `draftArtifact.relativePath`, uses an incompatible draft artifact kind, or requests draft proof for the Apple Foundation/CoreAI lane. The `mtp_quick`/`mtp-quick` smoke profile exists for a short, low-output plain-text validation pass; it is not a long stress benchmark and it is not a JSON-structure benchmark.
- Production provider resolution also reads verified installed-pack manifests, so a GGUF pack with a usable `draftArtifact` can surface MTP draft metadata without requiring `ROSS_LOCAL_DRAFT_MODEL_PATH`. Smoke helpers may still inject draft paths explicitly for simulator or copied-container validation.
- Hidden answer details show active draft tokens/model only when `draft_status=active`. Inactive candidates such as validator rejection, unsupported format, token/memory policy block, or degenerate output stay visible through `draft_status`, `draft_error_detail`, and `draft_model_path_type`; treat them as diagnostics only, not MTP proof.
- GGUF identity markers now include `draft_status` so standard-generation MTP attempts are diagnosable instead of ambiguous. Expected values include `active`, `no_draft_configured`, `draft_file_unavailable`, `draft_format_unsupported`, `draft_token_policy_blocked`, `draft_memory_policy_blocked`, `validator_rejected`, `validator_failed`, `draft_output_degenerate`, and `runtime_unavailable`.
- If the main GGUF runtime is available but draft validation fails, the identity marker stays `available=true` for the baseline runtime and reports a draft-specific `error` such as `draft_validator_rejected`, `draft_validator_failed`, `draft_token_policy_blocked`, `draft_memory_policy_blocked`, `draft_format_unsupported`, or `draft_file_unavailable`. `draft_error_detail` preserves only safe diagnostic categories such as `configured_acceleration=standard`, `requested_draft_tokens=4,max_supported_draft_tokens=2`, `main_bytes=5130000000,draft_bytes=79000000,max_combined_bytes=5184000000`, or a redacted validator error. Treat those as failed MTP proof, not failed GGUF proof.
- MTP validation uses strict draft setup even though normal GGUF generation may fall back to standard mode. A draft context that cannot initialize, or one that initializes but reports standard generation during required-draft proof, must surface as failed MTP validation (`draft_context_failed` or `draft_acceleration_inactive`), not as an active or benchmarkable draft path.
- If draft generation produces degenerate output, Ross returns `draft_output_degenerate`, clears the cached draft context, and quarantines that exact main+draft+token tuple for the current process. Later health checks report `draft_status=draft_output_degenerate` and later generation uses standard GGUF instead of repeatedly publishing active-but-bad MTP attempts.
- Simulator MTP proof also requires `--draft-model`; the helper now fails before launch if `--require-draft-acceleration` is requested without a draft artifact path, if GGUF/MTP proof receives a non-GGUF, wrong-header, or tiny placeholder draft artifact, if `--physical-memory-bytes` says a constrained E4B GGUF+MTP pair exceeds the app's draft memory budget, if MLX draft proof receives a file-like `.gguf`/`.safetensors`/`.bin` artifact or malformed MLX directory, or if CoreAI/CoreML asks for draft proof. As of June 20, 2026, `/Users/amanpandey/model-artifacts/mtp-gemma-4-12b-it.gguf` is locally present and checksum-verified. Current simulator builds prove active 12B MTP identity and the `mtp_quick` low-token cap, but they do not prove usable MTP generation: bounded 1024-context reruns with `draft_tokens=2` and `draft_tokens=1` both reached `draft_status=active`, then failed closed as `draft_output_degenerate` after emitting `<|channel>11111111111`. Those failed summaries preserve diagnostic token fields, draft model labels, and token speed, but they are not benchmark evidence. An earlier 1024-context rerun also exposed that a 128-token prompt batch was smaller than the 198-token source-bound smoke prompt and could assert before any terminal smoke marker. The non-constrained proof lane now keeps the 1024 context and 64-token physical batch while raising prompt batch to 256; constrained 7GB-class E4B remains at 128 to preserve its memory gate. No MTP benchmark pass or physical-device MTP acceleration number is claimed from this checkpoint.
- `docs/REAL_MODEL_QA_RESULTS.md` records the June 2, 2026 simulator GGUF smoke:
  - runtime: `gemma_local_runtime`
  - tier: `quick_start`
  - artifact: `/Users/amanpandey/projects/ross-gemma4/artifacts/gemma-2-2b-it-Q4_K_M.gguf`
  - SHA-256: `e0aee85060f168f0f2d8473d7ea41ce2f3230c1bc1374847505ea599288a7787`
- A June 19, 2026 current-build simulator GGUF full-profile smoke passed under the guarded benchmark summary path. This is simulator-only GGUF baseline evidence, not a physical-device result and not MLX/CoreAI/MTP proof:
  - runtime identity: `provider=AlphaLlamaCppProvider`, `requested_runtime=gemma_local_runtime`, `actual_runtime=gemma_local_runtime`, `model_format=local_model_artifact`, `artifact_path_type=file`, `fallback=none`, `available=true`
  - matrix: English source-grounded QA, Bengali/Hindi/Tamil/Telugu source-grounded QA, and an English no-document open query
  - elapsed: `54.44s`
  - stage speeds: source `11.14 tok/s`, general `10.92 tok/s`, Bengali `10.51 tok/s`, Hindi `10.07 tok/s`, Tamil `8.45 tok/s`, Telugu `9.33 tok/s`
  - stage token counts: source `207 in / 118 out`, general `190 in / 192 out`, Bengali `328 in / 121 out`, Hindi `278 in / 134 out`, Tamil `382 in / 96 out`, Telugu `433 in / 96 out`
  - stage first-token latency: source `12586ms`, general `13763ms`, Bengali `21958ms`, Hindi `18761ms`, Tamil `27691ms`, Telugu `32447ms`
  - all stages reported `*_native_model=true`, source refs were retained for every source-grounded stage, and all stage metrics were emitted in `ROSS_SMOKE_BENCHMARK_SUMMARY`
- A June 19, 2026 current-build physical iPhone 15 Pro GGUF full-profile smoke passed after installing the Debug build on `Aman's iPhone` (`iPhone16,1`) and seeding `gemma-2-2b-it-Q4_K_M.gguf` into `RossAlpha/model-packs/quickStart`. This is physical-device GGUF baseline evidence and storage-path proof for a seeded artifact, not app-download proof and not MLX/CoreAI/MTP proof:
  - runtime identity: `provider=AlphaLlamaCppProvider`, `requested_runtime=gemma_local_runtime`, `actual_runtime=gemma_local_runtime`, `pack_runtime=gemma_local_runtime`, `model_format=gguf`, `artifact_path_type=file`, `acceleration=standard`, `draft_status=no_draft_configured`, `context_tokens=10240`, `gpu_offload=n_gpu_layers:32,offload_kqv:true,op_offload:true`, `fallback=none`, `available=true`
  - matrix: English source-grounded QA, Bengali/Hindi/Tamil/Telugu source-grounded QA, and an English no-document open query
  - elapsed: `19.95s`
  - stage speeds: source `14.63 tok/s`, general `15.00 tok/s`, Bengali `14.03 tok/s`, Hindi `14.29 tok/s`, Tamil `10.26 tok/s`, Telugu `10.00 tok/s`
  - stage token counts: source `207 in / 105 out`, general `190 in / 190 out`, Bengali `328 in / 192 out`, Hindi `278 in / 192 out`, Tamil `382 in / 54 out`, Telugu `433 in / 60 out`
  - stage first-token latency: source `817ms`, general `1156ms`, Bengali `2091ms`, Hindi `1785ms`, Tamil `2325ms`, Telugu `2699ms`
  - memory markers: `resident_mb=2720 phys_footprint_mb=1112` at provider-ready and approximately `2763/1151` by the final Telugu stage
  - installed-pack listing after the run found seeded `quickStart` and `caseAssociate` GGUF proof packs plus a manifest-backed `quick_start` `gemma-4-e4b-q4` pack with `draft=mtp-gemma-4-E4B-it.gguf`
- A June 19, 2026 guarded physical iPhone 15 Pro MTP `mtp_quick` attempt against installed pack `gemma-4-e4b-q4` failed before generation and must not be counted as MTP proof:
  - primary artifact: `gemma-4-E4B-it-UD-Q4_K_XL.gguf`, manifest size `5.13 GB`, runtime `gemma_local_runtime`
  - draft artifact: `mtp-gemma-4-E4B-it.gguf`
  - failure: primary GGUF load reported `mmap failed: Cannot allocate memory`; identity stayed baseline GGUF with `acceleration=standard`, `draft_status=validator_failed`, `draft_error_detail=validator_error=couldNotInitializeContext`
  - guardrail: helper exited non-zero with `error=draft_acceleration_required`, so no MTP benchmark speed or draft token count is recorded from this device
- The smoke harness now reports native-model markers such as `bengali_native_model` and `hindi_native_model` so QA can distinguish direct multilingual model output from Ross's source-preserving fallback.

## Still Not Proven

Do not claim release-ready physical iPhone inference until these are recorded:

1. A configured multi-GB GGUF is downloaded through the app on a physical iPhone.
2. Resume/restart, provider size or checksum handling, validation, activation, repair, and deletion are observed on-device.
3. Imported PDF, image, and text files from Files/iCloud/Downloads are used in Ask Ross with source-grounded English, Hindi, and Bengali questions.
4. Device performance, storage use, privacy ledger entries, logs, and fallback behavior are recorded in `docs/REAL_MODEL_QA_RESULTS.md`.

The MLX lane remains unproven on physical iPhone hardware. It now supports both developer-supplied local directories and ZIP-packaged MLX installs, but physical-device validation is still pending.

As of June 19, 2026, the Quick Start MLX primary and assistant artifacts are locally materialized at `/Users/amanpandey/model-artifacts/gemma-4-E4B-it-qat-4bit` and `/Users/amanpandey/model-artifacts/gemma-4-E4B-it-qat-assistant-6bit`. Simulator preflight accepts both directory shapes and a simulator launch proves real MLX routing with `requested_runtime=mlx_swift_lm` and `actual_runtime=mlx_swift_lm`, but the current primary package is `Gemma4ForConditionalGeneration`/multimodal and is now fail-closed as `unsupported_model_archive` before generation. The bundled direct-MLX primary descriptors for `gemma-4-E4B-it-qat-4bit` and `gemma-4-12B-it-qat-4bit` are therefore not `releaseReady`; inventory still reports them as catalog hints, but the fetch planner blocks them with `catalog_primary_not_release_ready` until a compatible text-only MLX archive is identified. This is not an MLX generation benchmark; it is routing and crash-prevention evidence.

Before morning device validation, run `scripts/ios-runtime-artifact-inventory.sh` first. It prints `ROSS_RUNTIME_ARTIFACT_INVENTORY` rows for local GGUF, draft-like MTP GGUF, MLX directories, CoreAI/CoreML adapters, the CoreAI system sentinel, `catalog_gguf status=expected` rows for the configured tiered GGUF primaries, `catalog_mtp_draft status=expected` rows for the configured Gemma 4 MTP companion URLs/checksums, and `catalog_mlx status=expected` / `catalog_mlx_draft status=expected` rows for the configured MLX primary and assistant directories without launching Simulator, devicectl, or the app. Treat catalog rows as acquisition/readiness hints only; GGUF/MTP catalog rows include `repo`, `target_file`, `acquisition_hint=hf_download_gguf_file`, and preflight hints so the missing-artifact repair step is explicit, and MLX catalog rows include `repo`, `target_dir`, `acquisition_hint=hf_download_mlx_directory`, and `preflight_hint=simulator_mlx_directory_preflight`. Run `scripts/ios-runtime-artifact-fetch-plan.sh --tier <tier> --target-root ~/model-artifacts --physical-memory-bytes <bytes>` to print the exact dry-run `hf download ... --local-dir ...` commands plus simulator `--preflight-only` commands for the requested tier's GGUF primary, MTP draft pair, MLX primary/draft directories, and CoreAI sentinel/adapter. The morning checkpoint planner prints this same artifact-fetch plan as step `0` before any device command, so operators see the missing-artifact acquisition/preflight sequence before touching the cabled iPhone. The planner requires exact tier artifact filenames, so a local 12B GGUF/draft pair cannot satisfy a Quick Start E4B MTP plan; when the matching GGUF and draft are present it prints an `mtp_draft status=present action=preflight_pair` command with `--require-draft-acceleration --smoke-profile mtp_quick` and propagates `--physical-memory-bytes` so constrained E4B memory-fit gates run before the phone is touched. It also reports `downloader_status` and prints a `lane=downloader action=install` row when the Hugging Face downloader is missing. It does not download artifacts or launch any Apple runtime. The CoreAI system sentinel includes `runtime=apple_foundation_models`, `artifact_kind=system_model`, and `preflight_hint=simulator_system_model_preflight`; it is still `status=unknown` until OS runtime availability and generation are proven. MTP proof still requires a local or installed draft artifact plus active draft acceleration, and MLX proof still requires a downloaded local or installed MLX directory with config, tokenizer, non-empty safetensors weights, and a runtime-compatible text-only archive config. Local or installed MLX directories that only pass shape checks but declare unsupported multimodal, MoE, dense 31B, or non-draft assistant configurations are reported as missing with `reason=unsupported_model_archive` or `reason=unsupported_gemma4_assistant`, not benchmark-ready. Local and installed file-backed CoreAI/CoreML adapter rows must have non-empty reachable adapter contents. If you have a copied or mounted app container, pass `--installed-root <RossAlpha-support-root-or-model-packs-root>` as well; this adds manifest-backed rows such as `installed_gguf`, `installed_mtp_draft`, `installed_mlx`, `installed_mlx_draft`, and `installed_coreai`. Add `--physical-memory-bytes <bytes>` when planning for a known device class; constrained E4B GGUF+MTP pairs that exceed the app's draft memory budget are reported as `installed_mtp_draft status=missing reason=manifest_draft_memory_policy_blocked` with `main_bytes`, `draft_bytes`, and `max_combined_bytes` instead of being treated as launch-ready. Installed GGUF/MTP rows must pass GGUF header, size, checksum, and optional memory-fit checks; installed MLX rows must pass the same directory-shape and archive-compatibility checks used by local inventory, and malformed reachable artifacts are reported as `manifest_primary_unusable_artifact` or `manifest_draft_unusable_artifact` instead of being treated as ready. File-backed installed artifacts with a 64-character manifest SHA-256 also emit `checksum_status`; `manifest_primary_checksum_mismatch` and `manifest_draft_checksum_mismatch` are missing-lane results, not benchmark-ready artifacts. Then run `scripts/ios-morning-runtime-checkpoint-plan.sh --device <CoreDevice-id-or-name-or-UDID> --installed-root <RossAlpha-support-root-or-model-packs-root> --physical-memory-bytes <bytes>` to print the guarded command sequence and skip MTP/MLX/CoreAI lanes whose installed-pack inventory is missing, unusable, checksum-mismatched, or memory-blocked. Prefer the CoreDevice identifier printed by `xcrun devicectl list devices` when it differs from Xcode's iOS UDID. The MTP morning command is printed only when the same requested tier has both `installed_gguf status=present` and `installed_mtp_draft status=present`; a draft-only row is not enough to launch proof. The printed order starts with the local artifact-fetch dry-run, then `--list-only`, then GGUF baseline `quick_low_context` smoke, MTP `mtp_quick` with `--require-draft-acceleration`, and installed MLX/CoreAI lanes with the full varied document/query matrix: English source-bound document QA, Bengali, Hindi, Tamil, Telugu source-bound document QA, plus English open no-document query. The MTP plan intentionally does not pass `--allow-device-proof-pack`, so a seeded GGUF baseline cannot masquerade as installed MTP proof.

Local `lane=gguf` inventory intentionally excludes draft-like filenames such as `mtp-*`, `*draft*`, or `*assistant*`; a draft-only directory reports `lane=gguf status=missing reason=no_primary_gguf_file_with_header_and_size_over_1mb` while still reporting `lane=mtp_draft status=present`.

Local `lane=mlx` inventory intentionally excludes assistant/draft-like directories the same way: a downloaded MLX assistant directory reports `lane=mlx_draft status=present`, while primary MLX remains missing until a non-draft MLX directory is available. The fetch planner reports such draft-only MLX artifacts as `action=waiting_for_primary`, and if the catalog primary is blocked as not release-ready it reports the missing companion as `lane=mlx_draft status=blocked action=waiting_for_primary` instead of downloading a standalone draft directory. Once primary and draft are both present, it emits a paired `action=preflight_pair` command with `--require-draft-acceleration`; when a compatible primary is present but the draft still needs downloading, it emits `action=preflight_pair_after_download` using the primary as `--model` and the downloaded assistant directory as `--draft-model`.

The morning checkpoint plan applies the same primary-GGUF rule to `--gguf-model`: it skips the baseline seeded GGUF command with `missing_or_invalid_primary_gguf` for tiny/wrong-header files and `local_gguf_is_draft_like` for draft-like GGUF paths.

Any MLX benchmark must show `actual_runtime=mlx_swift_lm` in `ROSS_RUNTIME_IDENTITY`. Any Apple built-in/CoreAI/CoreML benchmark must show `actual_runtime=apple_foundation_models`. In this codebase, `coreai` and `coreml` are smoke/runtime aliases for Apple's Foundation Models path, not separate benchmark lanes. If either lane reports `actual_runtime=gemma_local_runtime`, the run is a GGUF fallback or routing error, not a valid MLX/CoreAI/CoreML benchmark.

The Apple built-in/CoreAI/CoreML debug-smoke path now treats `artifactKind=system_model` and `modelPath=system-model` or `system://...` as the system Foundation Models runtime, not as a missing adapter file. A June 19, 2026 simulator check produced a real identity marker with `provider=AlphaFoundationModelsLocalProvider`, `actual_runtime=apple_foundation_models`, and `artifact_path_type=system`; generation still failed in simulator, now categorized as `coreai_generation_failed`, so this is routing evidence only, not a CoreAI/CoreML generation benchmark. Empty plain-text Foundation/CoreAI answers are categorized separately as `coreai_empty_response` and remain retryable through the assistant fallback path instead of being published as successful CoreAI output. If the provider cannot become available before generation, the failed identity marker reports `provider=AlphaUnavailableRealLocalModelProvider` while keeping `actual_runtime=apple_foundation_models`.

CoreAI/Foundation identity markers report `acceleration=standard`, `draft_tokens=nil`, `draft_model=nil`, and `draft_status=not_supported`. That lane is system-managed; do not treat the absence of draft acceleration as an MTP-style failure, and do not count it as a benchmark unless generation succeeds under `actual_runtime=apple_foundation_models`.

Clear unavailable categories for benchmark triage:

- `missing_mlx_artifact`: the requested MLX lane did not receive a usable MLX directory path, including the case where a GGUF file was supplied to `--runtime mlx`.
- `invalid_mlx_artifact`: the requested MLX directory exists but lacks the required MLX runtime files.
- `unsupported_model_archive`: the requested MLX directory is structurally present but belongs to a Gemma 4 archive family the current runtime must not load directly, such as multimodal/conditional-generation Gemma 4, MoE/26B-A4B, or unsupported dense 31B. This is a fail-closed guardrail: do not count a crash-prone MLX package as generation proof.
- `invalid_mlx_draft_artifact`: the primary MLX directory is usable, but the configured draft companion is not.
- `mlx_generation_failed`: the MLX provider was selected with a usable MLX primary artifact, but standard generation failed before returning a usable answer.
- `mlx_draft_generation_failed`: MLX draft acceleration was requested in smoke proof mode, the primary/draft identity was staged, but speculative draft generation failed before a valid answer. In that mode Ross does not retry standard MLX generation, so a required-draft smoke cannot publish standard MLX numbers.
- MLX identity markers also carry `draft_status`; MLX draft acceleration is reported as `active` only when the primary runtime is available and the draft directory is usable. Invalid or unsupported draft companions stay `acceleration=standard` with a concrete `draft_status` instead of poisoning the whole MLX lane.
- When the primary MLX runtime is available but the draft companion is missing, invalid, or unsupported, `ROSS_RUNTIME_IDENTITY` keeps `available=true` for MLX and reports the draft-specific `error` category. Treat that as failed MLX draft proof, not failed MLX standard-generation proof.
- `missing_coreai_artifact`: a configured CoreAI/Foundation adapter path was required but could not be opened.
- `unsupported_runtime_on_platform`: the Apple built-in/CoreAI runtime is unavailable on the current OS, device, or build.
- `coreai_generation_failed`: the Apple built-in/CoreAI provider was selected and available, but a generation call failed before returning a usable answer.
- `coreai_empty_response`: the Apple built-in/CoreAI provider returned no usable plain-text content for a matter answer; treat this as failed generation, not a benchmark pass.
- `coreai_invalid_response`: the Apple built-in/CoreAI provider returned content for a structured task, but it did not satisfy the required response schema; treat this as failed generation, not a benchmark pass.
- CoreAI/CoreML failure outputs preserve the CoreAI execution path label, `acceleration=standard`, and packed input size when available. This is lane metadata for triage and Answer Details, not a successful CoreAI benchmark.

Ross now fails closed on malformed or known-bad Gemma 4 MLX archives instead of waiting for an inference-time crash:
- MLX install verification requires a directory with `config.json`, tokenizer metadata, and safetensors weights or an index before writing an installed-pack manifest.
- `gemma4_assistant` archives are still rejected as primary MLX targets, but they are now accepted as draft companions for speculative decoding on supported tiers.
- Gemma 4 26B-A4B MLX archives are rejected because the current upstream loader still does not support the MoE routing keys.
- Known Gemma 4 31B dense MLX archives are also treated as unsupported because the current stack can still crash on first generation.

If `ROSS_LOCAL_DRAFT_MODEL_PATH` points at an unsupported draft archive, Ross now drops back to standard MLX generation instead of poisoning the whole runtime. The exception is smoke proof mode (`ROSS_LOCAL_MODEL_SMOKE_REQUIRE_DRAFT_ACCELERATION=1`): once the MLX identity says draft acceleration is active, a draft-generation failure returns `mlx_draft_generation_failed` instead of retrying standard generation.

CoreAI/CoreML adapter artifacts are file-backed Foundation Models adapters, not the same thing as the built-in `system_model` sentinel. Persistence keeps zero-byte `system://` shortcut behavior limited to true system packs; CoreML/Foundation adapter packs must keep their installed artifact identity and fail as `missing_coreai_artifact` if the adapter cannot be opened.

Product surfaces use the same distinction: a file-backed CoreML/Foundation adapter is not labeled as "Built-in CoreAI" and does not suppress the real built-in system option when the OS reports that the system assistant is available.

## Build Notes

For local Swift verification:

```bash
swift test --package-path ios
scripts/test-ios-runtime-swiftpm.sh
```

For app integration, build and launch through Xcode or XcodeBuildMCP using the shared `Ross` scheme. Simulator success is useful evidence for compilation and integration, but it does not replace physical-device proof for model downloads, storage pressure, or hardware runtime behavior.
